import { DocumentFormatPipePipe } from './document-format-pipe.pipe';

describe('DocumentFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentFormatPipePipe(null);
    expect(pipe).toBeTruthy();
  });
});
