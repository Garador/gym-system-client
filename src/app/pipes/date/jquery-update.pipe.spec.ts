import { JqueryUpdatePipe } from './jquery-update.pipe';

describe('JqueryUpdatePipe', () => {
  it('create an instance', () => {
    const pipe = new JqueryUpdatePipe();
    expect(pipe).toBeTruthy();
  });
});
